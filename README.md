# vogon-poetry-parser
This script imports a Nessus CSV export, normalizes hostnames from DNS/NetBIOS formats, extracts all file paths (Windows and Linux) from Plugin Output, and exports results in the format: Plugin, Severity, Hostname, Path
